export default function addClass(...selectores) {
    if(e.target.matches(`#${home.id}`)||e.target.matches(`.${$home.className} *`)){
        console.log(e.target);
        console.log('click');
    }

    selectores.forEach(selector => {
        
    })


}